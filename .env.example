# Local operator secrets — copy to `.env` (git-ignored, never committed).
# GH_PAT: a GitHub token with `repo` + `workflow` scope, used only by scripts/release.sh
#         to push the release tag that triggers .github/workflows/release.yml.
GH_PAT=
